<?xml version="1.0" encoding="utf-8"?>
 
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application>
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <!-- Core Firebase App Initialization -->
            <meta-data android:name="app.getnuri.startup.FirebaseAppInitializer"
                android:value="androidx.startup" />
            <!-- Other Firebase Initializers -->
            <meta-data android:name="app.getnuri.startup.FirebaseAppCheckInitializer"
                android:value="androidx.startup" />
            <meta-data android:name="app.getnuri.startup.FirebaseRemoteConfigInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>
</manifest>
